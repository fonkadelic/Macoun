platform :ios, '10.0'

target 'UIKonf' do
  use_frameworks!

  pod 'SwiftGen', '~> 5.1' 
  pod 'Reusable', '~> 4.0'
  pod 'IBAnimatable', '~> 4.2'

  target 'UIKonfTests' do
    inherit! :search_paths
    # Pods for testing
  end
end
