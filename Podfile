platform :ios, '10.0'

target 'UIKonf' do
  use_frameworks!

  pod 'IBAnimatable', :git => 'https://github.com/JakeLin/IBAnimatable', :branch => 'swift3'
  pod 'Reusable'  

  target 'UIKonfTests' do
    inherit! :search_paths
    # Pods for testing
  end
end
